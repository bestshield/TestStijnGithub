name: C/C++ CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  test: 
    name: jobnaam

    runs-on: windows-latest

    steps:

    - uses: actions/checkout@v1
    - name: configure
      run: Documents\GitHub\TestStijnGithub\testetstt

